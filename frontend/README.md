## Frontend Assets

This directory contains a copy of the root-level web assets (index.html, css/, js/) used by Tauri for the desktop app build.

**Important:** When modifying the dashboard UI, update files in **both** the project root and this `frontend/` directory to keep them in sync.

Alternatively, use a build script or symlink to automate the copy.
